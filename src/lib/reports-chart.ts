/**
 * Compact chart sizing for Reports tabs — shorter charts keep the page scannable.
 */
export const REPORT_CHART_HEIGHT = 200;

/** Space reserved under angled category/name labels so the plot area stays usable. */
export const REPORT_CHART_ANGLED_XAXIS_HEIGHT = 72;

export const REPORT_PIE_OUTER_RADIUS = 70;
export const REPORT_PIE_INNER_RADIUS = 28;

/** Solid pie charts (no donut hole). */
export const REPORT_PIE_OUTER_RADIUS_SOLID = 72;
